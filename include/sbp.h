#ifndef _SBP_H
#define _SBP_H

#include <firmware_version.h>   // definition of firmware version macros
#include <wrapper.h>            // wrapper definitions for functions that already exist in the firmware
#include <structs.h>            // structures that are used by the code in the firmware
#include <helper.h>             // useful helper functions
#include <patcher.h>            // macros used to craete patches such as BLPatch, BPatch, ...
#include <sendframe.h>          // sendframe functionality
#include <udptunnel.h>

#include "local_helpers.h"

/**
 * YOUR CODE HERE
 * This file should contain SBP-specific parts. E.g., your custom protocol
 * struct header definitions should go here and all your function prototypes,
 * SBP-spcific typedefs, etc.
 */

#endif // _SBP_H
